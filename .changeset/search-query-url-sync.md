---
"@jitaspace/web": patch
---

The Search page now keeps your query in the address bar. Previously the box was filled in from a shared `?q=` link but never updated as you typed, so refreshing the page — or sending someone the URL you were looking at — lost the search.
