---
"@jitaspace/web": patch
---

Added a patch-notes card to the home page carousel, alongside the news banners. It announces the most recent EVE client build whose static-data changes have been recorded — how many things changed and when — and links straight to that build's full diff. Hiding it hides only that build: the card comes back on its own as soon as CCP ships a newer one.
