---
"@jitaspace/web": patch
---

Added a banner to the home page linking to the latest patch notes — the most recent EVE client build whose static-data changes have been recorded — showing how many things changed and when. Hiding it hides only that build: the banner comes back on its own as soon as CCP ships a newer one.
