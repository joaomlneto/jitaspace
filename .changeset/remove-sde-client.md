---
"@jitaspace/db": minor
---

Add the SDE-only columns the web app needs so it no longer has to call an external SDE API: `DogmaAttribute.attributeCategoryId`, and on `SolarSystem` the `luminosity`, `radius`, `positionX/Y/Z`, `wormholeClassId`, `factionId` and the `border`/`corridor`/`fringe`/`hub`/`international`/`regional` trait flags.
