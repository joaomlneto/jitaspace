---
"@jitaspace/db": minor
---

Added `CorporationActivity`, `MercenaryTacticalOperation`, and `FreelanceJobSchema` (+ `FreelanceJobSchemaTag`) models for the `corporationActivities`, `mercenaryTacticalOperations`, and `freelanceJobSchemas` SDE files. Requires a database migration.
