---
"@jitaspace/web": patch
---

Mail label filters now persist in the URL. Selecting labels on the Mail page updates the address bar, so a filtered mailbox survives a page refresh and can be shared or bookmarked — previously the filter always reset to empty on load.
