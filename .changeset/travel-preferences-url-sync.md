---
"@jitaspace/web": patch
---

Travel Planner route settings now persist in the URL. Your route preference (Shortest / More Secure / Less Secure / Custom) and any custom security penalties are kept in the address bar alongside the waypoints, so a shared route link now reproduces the route the sender actually saw — previously it silently fell back to Shortest and could show a different path.
