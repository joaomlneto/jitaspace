---
"@jitaspace/web": patch
---

Build pages in Change History now arrive complete. Every item name shows straight away instead of filling in one at a time (which could take minutes on a large build), and the file and localization lists open instantly. Long lists show their first rows until you choose "Show all". A build that doesn't exist now shows the not-found page.
