---
"@jitaspace/web": patch
---

Permission-gated pages (Wallet, Mail, Calendar, Skills, Assets, and others) no longer briefly flash a "grant permissions" prompt while your saved session is loading — they show a loading placeholder until your session is known. The Wallet page also holds its table's space while entries load, so it no longer jumps.
