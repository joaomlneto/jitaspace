---
"@jitaspace/web": patch
---

Market item pages (`/market/<item>`) now load with the item's name and icon shown straight away instead of the page snapping into place after it loads, and the order tables hold their space while live orders stream in — so the page no longer jumps. Each item page also gets its own title and description for sharing and search.
