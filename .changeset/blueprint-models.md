---
"@jitaspace/db": minor
---

Added `Blueprint`, `BlueprintActivity`, `BlueprintMaterial`, `BlueprintProduct`, and `BlueprintSkill` models (and the `BlueprintActivityType` enum) for EVE Online blueprints, with relations from `Type`. Requires a database migration.
