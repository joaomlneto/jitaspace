---
"@jitaspace/web": patch
---

Change history now names every tracked dataset. Site archetypes, dungeons, missions, epic arcs, landmarks, secondary suns, dogma units, material sets and type lists used to appear as raw keys like `secondarysun` in grey — on the index, the timeline chart and every build page — and their added and removed entries were miscounted as changes.
