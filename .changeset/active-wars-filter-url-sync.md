---
"@jitaspace/web": patch
---

Active Wars filters now persist in the URL. The status, "in combat"/"mutual"/"open for allies" toggles, sort field and direction, and the rows/table view are all reflected in the address bar, so a filtered or sorted view can be shared, bookmarked, and survives a page refresh.
