---
"@jitaspace/web": minor
---

Previously-loaded data — your mail, skill queue, wallet, assets, and contacts — now stays visible after a page reload and while you are offline, instead of showing empty until it refetches. This cached data is cleared automatically when you log out.
